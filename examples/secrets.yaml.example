---
# Copy to secrets.yaml and fill in. secrets.yaml is gitignored.

wifi_ssid: my-network
wifi_password: my-password

# Full reporting URL, path included.
# No API key needed: the device pairs itself via the server dashboard.
kegboard_reporting_url: https://kegbot.example.com/api/kegboard-event
